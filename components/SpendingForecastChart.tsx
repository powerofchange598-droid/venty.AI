
import React from 'react';

/**
 * @deprecated This component has been replaced by the new specific chart components
 * inside the BudgetScreen to match the new Dashboard design requirements.
 * Keeping file structure intact to avoid build errors if referenced dynamically,
 * but rendering nothing.
 */
const SpendingForecastChart: React.FC<any> = () => {
    return null;
};

export default SpendingForecastChart;
